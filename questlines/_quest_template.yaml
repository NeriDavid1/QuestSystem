# Copy this file when creating new quests in other questlines.
# Prefer QuestForge auto keys. Manual form: {lineKey}__qNN_snake_name.yaml
# Quest keys MUST be unique across the entire OpenWorld game (not just this line).
# Delete this comment block when done.

quest:
  id: line_key__q01_quest_id
  name: "Quest Display Name"
  level_required: 1
  giver_npc: npc_id
  prerequisite: null  # or line_key__q01_previous_quest
  summary: "One-line description for presenters and designers."
  status: draft  # remove when complete

rewards:
  xp: 0
  items: []
    # - id: item_id
    #   amount: 1

steps:
  - type: talk_to_npc
    npc_id: npc_id
    dialogue_id: npc_quest_start
  # Add more steps using types from _registry/systems.yaml
